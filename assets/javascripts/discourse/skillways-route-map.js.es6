/**
 * Links the path `/skillways` to a route named `skillways`. Named like this, a
 * route with the same name needs to be created in the `routes` directory.
 */
export default function () {
  this.route('skillways', { path: '/skillways' });
}
